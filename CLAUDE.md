用于生成一个包含"40 道题"的 HTML 训练程序（使用我上传的模板文件结构）
题目必须有情境、有题干，不允许空题。
适合基础水平一般的学生。

1. 你的任务

当我上传【试题图片】或【题目文字】时，你必须完成以下任务：

分析试题考查的知识点、学生常见误区、易错点、真实痛点（简短输出即可）。

根据所分析的痛点与知识点，生成 40 道训练题。

所有题目必须写入我提供的 HTML 模板中的题库结构（questions 数组）中。

2. 输出格式（必须遵守）
你最终必须输出两部分：
（A）试题分析（教师用）

结构如下：

【题目简述】

【考查知识点】

【学生易错点】

【真实痛点】

只写简明要点即可。

（B）完整 HTML 文件，基于我提供的模板修改 questions 数组，不改变结构

严格要求：

保持模板原样，不改动样式、JS、HTML 框架。

只替换 const questions = [...] 这一个数组内容。

questions 数组必须包含 40 道题。

题型必须严格 choice → cloze → choice → cloze → … → 共40题。

每题结构必须如下：

✔ 选择题格式
{
  type: "choice",
  stem: "【标签】题干内容，必须有情境描述。",
  options: ["A", "B", "C", "D"],
  correctIndex: 1
}

✔ 填空题格式（拖拽填空）
{
  type: "cloze",
  stem: "【标签】题干内容，包含一个 ______ 空格。",
  blankId: "b1",
  options: ["正确项","干扰项1","干扰项2","干扰项3"],
  correctOption: "正确项"
}


必须有 stem，不允许为空。
不允许生成没有内容的题。

3. 题目生成标准（必须遵守）

题目必须“能考查学生痛点、易错点”，而不是机械算术题。

题干必须带情境，例如：

小明进行实验

生活场景

简单实验操作

学生常犯的错误情境对比

每题必须不同，不能重复模板。

难度分布：

1–13：基础纠错题（单位、定义、概念）- 适合基础水平一般的学生打牢基础

14–27：中等情境题 - 逐步提升，计算保持简单

28–40：综合题 - 多步骤推理但计算不复杂，重理解应用

4. 你生成 HTML 的方式

当我输入题目图片或文字时，你必须：

先给出试题分析（A 部分）

再输出完整 HTML（B 部分），格式必须为：

<!DOCTYPE html>
<html>...（保持我提供的模板结构不变）...</html>


模型不允许使用“省略”“...” 等。必须输出可直接保存并运行的完整文件。

5. 唯一禁止的行为

不允许输出空 questions 数组。

不允许输出无 stem 的题目。

不允许省略 HTML 内容。

不允许自行改动模板逻辑或样式。

不允许改变题型轮换顺序。
